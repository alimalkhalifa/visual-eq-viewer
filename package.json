{
  "name": "visual-eq-viewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.3.1",
    "rc-slider": "^8.7.1",
    "react": "^16.10.0",
    "react-bootstrap": "^1.0.0-beta.14",
    "react-dom": "^16.10.0",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.1.2",
    "three": "^0.137.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "rm -rf build && yarn build && rm -rf /var/www/html/static && cp -r build/* /var/www/html/"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
